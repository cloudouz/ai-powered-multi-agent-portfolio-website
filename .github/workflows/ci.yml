name: CI
on:
  push: { branches: ["**"] }
  pull_request:

permissions:
  contents: read

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run: { working-directory: backend }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install -U pip bandit safety pip-audit
      - run: pip install . fastapi uvicorn pydantic-settings httpx slowapi python-jose[cryptography] argon2-cffi sqlalchemy sqlmodel redis markdown-it-py langchain-core langchain-community langchain-openai langchain-google-genai google-generativeai tavily-python
      - run: bandit -r app -q || true
      - run: safety check -i 51457 || true
      - run: pip-audit || true
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run: { working-directory: frontend }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20" }
      - run: npm ci || npm install
      - run: npm run lint
      - run: npm audit --audit-level=high || true
